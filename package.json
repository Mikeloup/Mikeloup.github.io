{
  "name": "tandemtv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Site Tandem TV — généré automatiquement depuis la chaîne YouTube @tandem_tv",
  "scripts": {
    "build": "node build.mjs",
    "demo": "node build.mjs --demo",
    "serve": "node --run build && node serve.mjs",
    "preview": "node serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
